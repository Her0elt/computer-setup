- name: Install System Packages 1 (build-essential, cmake, pkg-config, pthread)
  homebrew:
    name: ["build-essential", "cmake", "pkg-config", "libpthread-stubs0-dev"]
  tags:
    - install
    - nvim
    - core

- name: Install System Packages 2 (lua, unzip, libtool, libtool-bin, gettext)
  homebrew:
    name: [ "feh", "lua5.1", "unzip", "libtool", "libtool-bin", "gettext"]
  tags:
    - install
    - nvim
    - core
- name: Install System Packages 3
  become: true
  homebrew:
    name: ["lsb-release", "gnupg","curl"]
  tags:
    - core
    - install
    - docker
- name: Install Clang
  become: true
  homebrew:
    name: ["clang", "clang-11", "clang-12", "clangd", "clangd-11", "clangd-12"]
  tags:
    - install
    - nvim
    - core

- name: Install System Packages 4
  become: true
  homebrew:
    name: ["compton","nitrogen","jp", "compton", "htop", "lsof", "fzf", "tmux","i3", "blueman","ccache", "ninja-build", "python3-pip", "dconf-editor", "nvidia-settings", "pavucontrol", "moreutils", "clangd", "ubuntu-mate-desktop" ]
  tags:
    - install
    - core


